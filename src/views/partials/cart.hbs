{{#if (eq userType 'user')}}
<!--
<div class="w3-sidebar w3-bar-block w3-border-right" style="width: 30%; display:none; right:0" id="mySidebar">
  <button onclick="w3_close()" class="w3-bar-item w3-large">Close &times;</button>
  <p class="w3-bar-item">Total: <span class="w3-text-teal">${{cart_cost.total}}</span></p>
  <div class="scrollable-sidebar">
  {{#each cart}}
    <div class="w3-margin-top">
      <div class="w3-bar-item">
        <a href="/books/{{ISBN}}" class="w3-block w3-button fb-palette-fourth"><p>{{title}}</p><p>{{state}}</p><p>${{cost}}</p></a>
        <a href="/books/removeFromCart/{{id}}" onclick="this.parentElement.remove()" class="w3-block w3-red w3-hover-black w3-padding-small">remove</a>
      </div>
    </div>
  {{/each}}
  </div>
  <a href="#" class="w3-button sidebar-button w3-block fb-palette-primary">Buy</a>
</div>
-->

<div class="w3-sidebar w3-bar-block w3-card w3-animate-right" style="display:none; right:0" id="mySidebar">
    <button class="w3-bar-item w3-button w3-large" onclick="w3_close()">Close &times;</button>
    <button class="w3-bar-item w3-button" id="item" onclick="remove(1)">Link</button>
    
    {{#each cart}}
    <div class="w3-bar-item w3-padding-small">
      <span class="w3-padding-small w3-button">{{title}} {{state}} {{cost}}</span>
      <a href="#" class="w3-button w3-red w3-padding-small w3-right">Remove</a>
    </div>
    {{/each}}
    
</div>

{{/if}}